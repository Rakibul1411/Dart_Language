void main() {
  var city = ['Dhaka', 'Khulna', 'Kushtia'];

  var result = city.length;
  print(result);

  var cityName = city.first;
  print(cityName);

  cityName = city.last;
  print(cityName);

  var cityNames = city.reversed;
  print(cityNames);

  var check = city.isEmpty;
  print(check);

  check = city.isNotEmpty;
  print(check);
  
}